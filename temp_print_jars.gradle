tasks.register("printFabricJars") {
  doLast {
    configurations.clientCompileClasspath.files.findAll { it.name.contains("lifecycle") || it.name.contains("networking-api") }.each { println it }
  }
}
